import api from './api';
import getActions from './actions';
import reducer from './reducer';
import * as selectors from './reducer';
import middleware from './middleware';

export default { api, getActions, reducer, selectors, middleware };
